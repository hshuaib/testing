<%= form_for  @member, html: {multipart: true} do |f| %>
  <% if @member.errors.full_messages.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@member.errors.count, "error") %> prohibited this member from being saved:</h2>

      <ul>
      <% @member.errors.full_messages.each do |message| %>
        <li><%= message if @member.errors.full_messages.first == message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field fonty formpad">
    <%= f.label :name, "الاسم" %><br>
    <%= f.text_field :name, :required=> true %>
  </div>
  <div class="field fonty formpad">
    <%= f.label :role, "المنصب" %><br>
    <%= f.text_field :role, :required=> true %>
  </div>
  <div class="actions fonty formpad">
    <%= f.submit "حفظ" %>
  </div>
<% end %>
